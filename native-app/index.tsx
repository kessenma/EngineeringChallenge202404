import { AppRegistry } from 'react-native';
import App from './App'; // Adjust the import path as needed
console.log('Registering RootNavigator with AppRegistry');
AppRegistry.registerComponent('main', () => App);